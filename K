local Keys = {"Silent Wiining","Nigga"}
local Key = _G.Key

if not typeof(Key) == "string" then
    while typeof(Key) == "string" do
        
    end
end

if table.find(Keys,Key) then
    print("Whitelsited")
    return
end

print("Not Whitelisted")
Key = nil
_G.Key = nil
